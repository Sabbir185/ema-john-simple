import React from 'react';

const Inventory = () => {
    document.title = 'Inventory';

    return (
        <div>
            <h1>This is inventory</h1>
        </div>
    );
};

export default Inventory;